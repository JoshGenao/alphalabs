=== SESSION SRS-MD-006 ===
Date: 2026-07-17
Feature: SRS-MD-006 — execute a startup readiness check before enabling live trading (SyRS SYS-76, NFR-R6; StRS SN-2.04/SN-2.05). AC: live startup blocked until IB connectivity/auth, IB account data, SSD access, ingestion freshness ≤1 trading day, service health, NAS-or-degraded-alert all pass; paper needs subscription manager + sim engine; failures alert + hold pre-trade unless overridden (override alerts).
Outcome: serialized (code integrated; passes stays false — the real IB Gateway round-trip leg is integration-gated; see deferred[])

What I did:
- `atp_readiness/runtime.py` — the pure SYS-76 fold. Reuses `atp_reliability.restart`'s SubCheck vocabulary + `is_subcheck_satisfied` (made public there; single source of the NAS degraded-only-with-alert rule). `build_runtime_report`: required set derived from REQUIRED_SUBCHECKS (missing sub-check ⇒ error, duplicate raises), one operator alert per failure AND for accepted NAS degraded-mode, alert sink REQUIRED keyword-only with propagating failures. `ingestion_is_fresh`: fresh iff frontier ≥ close of the most recent completed trading session (exact ±1 ns; None ⇒ stale; calendar port injected, calendar failure raises). `fold_service_health`: five-service enum authority, absent/unknown ⇒ FAIL. `assert_paper_ready_or_hold` (both prerequisites, missing key ⇒ hold + alert). `release_hold_with_override` (mandatory OPERATOR_OVERRIDE alert delivered FIRST, then the audited transition — an undeliverable alert leaves the hold in place). `evaluate_runtime_readiness` re-runs BOTH halves per poll with override-lifetime semantics (see R3).
- `gate.py` + one token-free sibling method `assert_runtime_ready_or_hold`: merges the separately-tracked STATIC report with the runtime report through the same pinned transition table (static tracked separately so a previous fold's failures never feed back — runtime failures clear on recovery). ERR-9 leakage rule + all 25 SDK checks + 46 existing tests still pass.
- `probes.py` — I/O edge: `TierCliStorageProbe` (data008_tier_cli report; NAS unreachable ⇒ DEGRADED with alert_raised bound to the dispatch actually succeeding), `CoverageFrontierSource` (data011_coverage_cli; stalest watchlist symbol wins; empty watchlist refused), `FixtureIbProbe` / `EvidenceFileIbProbe` (committed ib_paper_account_evidence.json), service/paper fixtures (absent ⇒ unhealthy), `UsEquityCalendarAdapter` (function-local atp_strategy import — boot_evidence precedent), recording + durable JSONL alert sinks.
- `handlers.py`/`wiring.py` — the launch-surface consumer: `readiness wait` (the SDK-pinned CLI operation deferred to this feature) now real; polls both halves until READY/OVERRIDDEN or timeout (504→TIMEOUT exit code); `wire_readiness` all-required kwargs (no fixture fallback).
- `runtime_cli.py` — operator demonstration CLI (fail-closed allowlist parser; exit 0/1/2; JSON or k:v output; persisted JSONL alert inspection).
- `startup_readiness_runtime_contract` block + rewrite of the SDK contract's deferred entry 4; `tools/startup_readiness_runtime_check.py` (static parity + behavioural fail-closed checks) wired into ci.yml + run_ci_locally.sh + init.sh in the same slot.

What I tested (per step):
  Step 1: PASS — ./init.sh → "✓ Environment ready" (now also runs the runtime check).
  Step 2: PASS — fixture CLI workflows: real data008/data011 CLIs against tmp SSD/NAS stores; `python -m atp_readiness.runtime_cli` all-pass ⇒ state:ready exit 0; IB fixture failure ⇒ pre_trade_blocked exit 1 + persisted subcheck_failure alert (JSONL inspected); NAS dir missing ⇒ nas_archival:degraded:alerted + READY + nas_degraded_mode alerts; clock advanced 9 days ⇒ data_layer_ssd:fail (stale frontier) ⇒ blocked; override flags ⇒ state:overridden exit 0 + operator_override alert last in the trail; malformed args ⇒ exit 2.
  Step 3: PASS (fixture/mock contexts) — domain tests walk every sub-check through the real operator runtime's `readiness wait` (each single failure blocks with the failure addressable in the held payload; all-pass ⇒ exit OK + READY; mid-wait recovery unblocks); NAS degraded requires the alert; stale-beyond-one-trading-day holds; paper gate needs BOTH prerequisites; failure holds unless override (which alerts); raising sink surfaces.
  Step 4: PASS (as serialized) — passes STAYS FALSE: the genuine IB Gateway round-trip for SYS-76(a)+(b) is tests/integration/test_srs_md_006_ib_round_trip.py (ATP_RUN_INTEGRATION, paper port 4002 — forbidden in parallel sessions). Verification on the final tree: cargo test --workspace 1700 passed; pytest -m "not integration and not e2e" 3779 passed (incl. 28 unit + 7 boundary + 8 contract + 7 domain new); both readiness check tools PASS; mypy/ruff clean on touched files (repo-wide mypy/format baseline drift on untouched files pre-exists).

Critic verdicts:
  deterministic: APPROVE — no findings (staged + --range origin/main..HEAD)
  judgment (codex_review.sh): needs-attention at R4 with a SINGLE residual = the deferred launch-path enforcement (the documented non-convergence-on-deferred-runtime-slices case; landed serialized per its own alternative remedy). Convergence history, every in-scope finding fixed + regression-tested in the same commit:
    R1: (a) evidence probe substring-matched "ok" (a "not ok"/FAILED line could pass) → strict result-line parse (exact "test result: ok." prefix + zero failed); (b) a paper-prerequisite hold exited 0 → paper hold now fails the command (exit 1).
    R2: (a) a STALE evidence file could satisfy the gate (proves a past session, not current connectivity) → generated_at freshness bound (EVIDENCE_MAX_AGE_NS = 15 min, future-stamped/missing-stamp fail closed; live in-process IB probe named deferred); (b) override committed BEFORE its mandatory alert (a released hold could exist without the operator alert) → alert-delivered-FIRST ordering; an undeliverable alert leaves the hold in place (domain-regression-tested).
    R3: (a) launch-gating over-claim → narrowed to the ADVISORY readiness-wait surface in contract + docstrings (in-process launch enforcement = deferred SRS-ORCH-004/SRS-EXE-001); (b) an audited override was demoted by the next poll re-observing the same runtime condition → override-lifetime semantics (override survives runtime re-evaluation with failures still alerting; static config regression demotes; clean evaluation promotes to READY) — domain-tested; (c) coverage-frontier probe leaked I/O exceptions → unobservable frontier folds to None ⇒ stale ⇒ hold.
    R4 (final, recorded verbatim): "the shipped launch surface remains either unwired or explicitly advisory... Wire the readiness gate into the actual live-designation/strategy-container launch path" — that enforcement point DOES NOT EXIST in the repo yet (strategy start / live designation are deferred 501s owned by SRS-ORCH-004/SRS-EXE-001); the default `python -m atp_runtime` 501 for readiness-wait is pinned as an explicit deferred[] entry (uncovered capability ⇒ no public surface until a composer wires real probes). Serialized, passes stays false.

Resume / next — WHAT FLIPS THIS COMPLETE: run the ATP_RUN_INTEGRATION IB round-trip leg in an exclusive live session (single-live invariant) to demonstrate SYS-76(a)+(b) from a genuine gateway; then flip via verified-e2e/close_feature. Deferred[] owners in startup_readiness_runtime_contract: orchestrator/live-designation launch consultation (SRS-ORCH-004/SRS-EXE-001 — consume `readiness wait` or the gate directly), SRS-NOTIF-001 email+SMS behind AlertSink, per-service liveness substrate for SYS-76(e), SRS-LOG-001/UI-001/API-001 sinks. Do NOT rebuild the fold/probes/CLI — wire producers.
