=== SESSION SRS-SEC-001 ===
Date: 2026-07-07
Feature: SRS-SEC-001 — encrypt brokerage + notification credentials at rest; prevent plaintext credential logging (NFR-S1 / NFR-S4 / StRS C-3, SN-1.12)
Outcome: serialized (mechanism built + solo-proven; production container-runtime vault-decrypt for the Rust core is a deferred live-docker integration)

What I built:
- ATP_IB_ACCOUNT: new secret-flagged config key (the NFR-S1 brokerage account identifier). runtime_services.json catalogue + required_env_vars, .env.example, docker-compose x-atp-env, atp_config/README + DEPLOYMENT.md (count 18->19), test literal fixes.
- Encryption at rest — python/atp_config/vault.py: CredentialVault seal/open with Fernet (AES-128-CBC+HMAC-SHA256); key from a 0600 key-file OR scrypt passphrase; durable atomic write (scratch O_EXCL 0600 -> fsync -> os.replace -> dir fsync; missing dir fails closed); fail-closed on wrong key/tamper. load_vault_into_env (opt-in via ATP_VAULT_FILE) overlays the vault onto env; WHITELISTED to catalogued secret keys (rejects a vault carrying non-secret config, e.g. ATP_ENV — can't downgrade prod). Minimal CLI (generate-key / seal / status; never prints values).
- Log redaction — python/atp_logging/redaction.py: SecretRedactor (value-based substring scrub of message/correlation_id + STRATEGY event_type/strategy_id; pattern fallback for api_key=/passphrase=/db-/sk-/AKIA/ bare IB U#######). DEFAULT_REDACTOR = always-on pattern floor (never zero redaction). Wired into JsonlLogStore.write (persistence boundary — always redacts) + RoutedLogDispatcher.dispatch + build_separated_log_dispatcher. atp_config.secret_values(env) added. atp_logging stays free of any atp_config import (log_record_check-enforced).
- Value-aware production boot — python/atp_logging_boot.py: build_boot_log_dispatcher overlays the vault + builds SecretRedactor(secret_values(env)) + registers the vault-unlock secrets (passphrase + key material), so arbitrary (unshaped) IB/SMTP/SMS VALUES are masked. Named as secure_boot_wiring in the log_persistence_contract + README.
- Production enforcement — ReadinessGate.from_env/reevaluate now overlay the vault (fail-closed to PRE_TRADE_BLOCKED on a broken vault) AND reject plaintext catalogued secrets in staging/production (must be vault-sealed); dev keeps plaintext flexibility.
- Compose delivery + isolation — docker-compose.yml: read-only vault mount (${ATP_SECRETS_DIR}:/run/atp-secrets:ro) in *atp-volumes for the 9 credential-consuming services; notification-dispatcher added; x-atp-no-secrets anchor blanks ALL 5 secrets + vault key-file + passphrase for the 3 isolated services (jupyter / IB-gateway / strategy-runtime) which also get NO vault mount (SRS-SEC-004 least-privilege).
- Automated checks — tools/credential_security_check.py (8 checks: catalogue-secret / vault-ciphertext-fail-closed / redaction-wired / default-never-zero / boot-factory-value-aware / prod-secrets-must-be-vaulted / prod-log-wiring-value-aware / redaction-no-upstream-import); wired into ci.yml + run_ci_locally.sh loops. tools/deployment_check.py extended with per-service vault-wiring + anchor-blank assertions. SECURITY.md + DEPLOYMENT.md credential-handling docs.

What I tested (per step):
- Step 1: PASS — ./init.sh -> "✓ Environment ready" (installs cryptography 45.0.7). init.sh skips requirements-dev.txt -> pip install -r requirements-dev.txt for pytest.
- Step 2 (inspection/checks): PASS — credential_security_check PASS (8 lines); deployment_check PASS (per-service vault wiring); log_record/log_persistence/config/architecture checks PASS.
- Step 3 (AC): PASS (solo) — L7 tests/domain/test_credential_redaction.py: vault file is ciphertext + round-trips; IB/SMTP/SMS (+ strategy_id + passphrase) never in persisted logs on dispatcher AND direct-store AND value-aware-boot paths; default floor masks shaped tokens + bare IB; readiness consumes vault; prod plaintext secret BLOCKED; vault can't downgrade mode. L1 test_secret_vault / test_secret_redactor; L2 test_redaction_never_leaks. Full suite: 3088 passed, 3 pre-existing skips.
- cargo test --workspace: PASS (no Rust changes). ruff/mypy: clean (no new errors in my files; pre-existing schema.py:141 + main mypy-red untouched).

Critic verdicts:
  deterministic (critic_check.py --staged): APPROVE — no findings.
  judgment (adversarial_review.py, reviewer=codex): 11 rounds. Rounds 1-10 each surfaced a genuine gap, ALL FIXED: (1) vault-not-in-readiness + strategy_id-unredacted; (2) default-store-zero-redaction; (3) boot-factory-not-value-aware; (4) passphrase + bare-IB unredacted; (5) prod-encryption-optional; (6) compose-vault-not-mounted; (7) deploy-docs + contract-name-wrong-builder; (8) credential-consumers-lack-mount; (9) shared-env-leaks-secrets-to-isolated; (10) vault-overlays-non-secret-config + passphrase-not-blanked. Round 11 (still BLOCK): "vault-backed secrets not decrypted into CORE RUNTIME (Rust) startup" — the phase1 Rust images are `cargo test` STUBS (rust:1.75-slim, no Python, no production binary), so a Rust-service startup path that runs load_vault_into_env is a DEFERRED live integration requiring the real production runtime + docker build/run (NOT solo-parallel-safe). Stopped the loop here (memory: adversarial loops don't converge on deferred-runtime slices — scope + get human authorization, never fake APPROVE).

Resume / next (DEFERRED, named owners):
  - Container-runtime vault decrypt for the Rust core: a production entrypoint that runs load_vault_into_env (or a Rust vault loader) before launching the real service binary, verified against the phase1 compose profile. Owner: the real production runtime (current phase1 images are cargo-test stubs) + SRS-ARCH-004 deployment. Needs docker build/run (single-live, not parallel-safe).
  - build_boot_log_dispatcher must be wired by the SRS-LOG-001 runtime boot as the production log dispatcher (value-aware). Owner: SRS-LOG-001 runtime half.
  - passes stays FALSE until the operator finishes the live container-runtime verification (or via verified-e2e).
