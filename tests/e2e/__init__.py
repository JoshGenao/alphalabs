"""L6 — End-to-end tests.

Catches: UX regressions, websocket reconnect, kill-switch round-trip from
operator click -> execution. Playwright against the dashboard once it exists.
Mark with @pytest.mark.e2e.
"""
