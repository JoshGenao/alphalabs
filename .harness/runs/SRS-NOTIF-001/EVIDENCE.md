# SRS-NOTIF-001 — verification evidence

> Verify that notify the operator through email and push notification for IB connectivity loss and critical failures.

- **method**: `integration`
- **steps evidenced**: 4/4
- **critics**: deterministic `approve`, judgment `block`
- **artifacts**: 0
- **record complete**: NO

## Outstanding

- deterministic critic approved at c8cbe20f, but 76 non-evidence path(s) have changed since (.env.example, .github/workflows/security.yml, .gitleaks.toml…) — the approval describes code this close would not ship. Re-run the deterministic review.
- judgment critic verdict is 'block', not 'approve'
- step(s) [1, 2, 3, 4] ran against code that has since moved (97 path(s) changed, e.g. .env.example, .github/workflows/security.yml, .gitleaks.toml) - re-run them with `tools/evidence.py run SRS-NOTIF-001 --step N -- <command>`

## Acceptance criterion

> Step 3: Verify acceptance criteria: Notification dispatch begins within 60 seconds of detection and delivery status is stored as a notification event.

## Steps

### Step 1

Step 1: Run ./init.sh and confirm the development environment reports Environment ready before verification begins.

`pass` · exit `0` · executed by the tool

```
./init.sh
```

<details><summary>observed output (tail)</summary>

```
→ Installing dependencies...
  Using /Users/joshgenao/Documents/Programming/Python/alphalabs-wt-SRS-NOTIF-001/.venv/bin/python (Python 3.13)
→ Starting dev server...
  A server is already reachable at http://127.0.0.1:3000; using it for smoke tests.
→ Waiting for server...
→ Running baseline smoke test...
→ Running contract checks (scope=env)...
→ contract checks (scope=env, 17 check(s))
  · architecture_check
  · dependency_boundary_check
  · config_check
  · startup_readiness_gate_check
  · startup_readiness_runtime_check
  · ib_adapter_check
  · rest_api_check
  · websocket_api_check
  · cli_check
  · operator_workflow_surface_check
  · operator_interface_runtime_check
  · log_record_check
  · log_persistence_check
  · subscription_fanout_check
  · sim_halt_check
  · strategy_api_indicators_check
  · strategy_api_documentation_check
✓ 17/17 contract check(s) passed (scope=env)
✓ Environment ready
```

</details>

### Step 2

Step 2: Exercise SRS-NOTIF-001 using bash, file reads, dev server requests, and browser automation with the fixtures, mocks, or operator controls needed by the requirement.

`pass` · exit `0` · executed by the tool

```
python -m pytest tests/unit/test_deployment_egress_relay.py tests/domain/test_notification_egress_contract.py -q
```

<details><summary>observed output (tail)</summary>

```
...........                                                              [100%]
11 passed in 0.36s
```

</details>

### Step 3

Step 3: Verify acceptance criteria: Notification dispatch begins within 60 seconds of detection and delivery status is stored as a notification event.

`pass` · exit `0` · executed by the tool

```
python -m pytest tests/domain/test_notification_dispatch.py tests/domain/test_notification_transports.py -q
```

<details><summary>observed output (tail)</summary>

```
.......................                                                  [100%]
23 passed in 3.15s
```

</details>

### Step 4

Step 4: Record objective evidence from fault injection, integration test and leave passes false until the evidence proves the requirement end to end.

`pass` · exit `0` · executed by the tool

```
python3 tools/deployment_check.py
```

<details><summary>observed output (tail)</summary>

```
SRS-ARCH-004 PASS
- SRS-ARCH-004 Phase 1 deployment evidence:
- compose declares 13 Phase 1 services under profile 'phase1'
- phase1-dashboard-api binds 127.0.0.1 (SRS-SEC-002)
- compose passes 24 env vars to Phase 1 services
- SSD primary tier and NAS archive tier mounted via ATP_SSD_DATA_DIR / ATP_NAS_DATA_DIR
- 6 Dockerfiles present (core-runtime, strategy-python, dashboard-api, jupyter, ib-gateway, notification-egress)
- .env.example enumerates all 24 required env vars
- docs/DEPLOYMENT.md documents Phase 1 target, cloud VPS as future target, and portability constraints
- compose delivers the SRS-SEC-001 vault + credentials to all 9 credential-consuming services and blanks both from 3 isolated services (jupyter / strategy / IB Gateway)
- ATP_NTFY_UPSTREAM is unset or empty (iOS wake-up disabled; valid)
- phase1-notification-egress advertises AUTH on the plaintext hop (smtpd_sasl_auth_enable=yes, smtpd_tls_auth_only=no) as smtp.rs requires
- phase1-notification-egress requires SASL on both restriction lists and never permits mynetworks
- phase1-notification-egress publishes no host port, merges no ATP environment, mounts no vault, and holds only ATP_SMTP_API_KEY
- docker/notification-egress.Dockerfile pins its base image by digest
```

</details>

---

Generated by `tools/evidence.py render`. `passes: true` requires either every step executed by the tool with both critics approving, or a named human attestation — see `AGENTS.md`.
